import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  courseTable: {
    width: "100%",
  },
  paper: {
    width: "100%",
    borderRadius: 8,
    marginBottom: 24,
  },
});
