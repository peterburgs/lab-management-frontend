import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
  registrationTable: {
    width: '100%',
  },
  table: {
    minWidth: 700,
  },
  paper: {
    width: '100%',
    borderRadius: 8,
    marginBottom: 24,
  }
});