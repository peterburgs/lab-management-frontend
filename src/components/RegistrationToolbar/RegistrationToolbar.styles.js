import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  paper: {
    padding: "0.5rem",
    borderRadius: 8,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  button: {
    borderRadius: 8,
    textTransform: "none",
  },
});
