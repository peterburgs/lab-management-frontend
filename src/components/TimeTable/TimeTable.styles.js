import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  timeTable: {
    marginTop: "1rem",
  },
  navigation: {
    display: "flex",
    width: '100%',
    justifyContent: 'flex-end',
  }
}));
