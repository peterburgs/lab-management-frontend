import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  formControl: {
    margin: `0 0.5rem`,
    minWidth: 120,
  },
  formElement: {
    marginBottom: "1rem",
    width: "100%",
  },
  dialog: {
    borderRadius: 8,
  },
});
