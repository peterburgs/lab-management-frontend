import { makeStyles } from "@material-ui/core";

export default makeStyles({
  wrapper: {
    display: "flex",
    justifyContent: "center",
  },
  spinner: {
    color: "white",
    marginTop: -12,
    marginLeft: -12,
  },
});
